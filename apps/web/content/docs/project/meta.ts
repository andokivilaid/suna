import { defineMeta } from 'blume';

export default defineMeta({
  title: 'Your project',
  pages: ['index', 'manifest', 'agents', 'knowledge', 'permissions', 'models', 'secrets', 'legacy-toml'],
});
